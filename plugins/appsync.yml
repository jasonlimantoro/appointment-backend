appSync:
  name: ${self:custom.stage}-appointment-appsync
  authenticationType: AMAZON_COGNITO_USER_POOLS
  userPoolConfig:
    defaultAction: DENY
    userPoolId:
      Ref: CognitoUserPool
  schema:
    - schema/guest/types.graphql
    - schema/entry/types.graphql
    - schema/root.graphql