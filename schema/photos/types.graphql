type Photo {
  id: String!
  entry: Entry!
  key: String!
  createdAt: String!
}

input CreatePhotoInput {
  id: String
  entryId: String!
  key: String!
}

extend type Mutation {
  createPhoto(input: CreatePhotoInput!): Photo!
}
